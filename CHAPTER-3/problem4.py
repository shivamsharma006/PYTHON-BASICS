# Replace the double space from problem 3 with single spaces

line = "Shivam is a  good  boy"
print(line.replace("  "," "))
# original string not changed
# Strings are immutable:- which means string not changed
print(line)